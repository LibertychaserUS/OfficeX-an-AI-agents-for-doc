# Sandboxes

This directory is reserved for per-run mutable document-edit sandboxes.

Rules:

- do not treat files here as imported source of truth
- do not promote outputs directly without audit and approval
- create one bounded sandbox per run or editing session
- prefer deleting obsolete sandbox copies over reusing stale mutable state
