from pathlib import Path

SCAFFOLD_ROOT = Path(__file__).resolve().parents[2]
PROJECT_ROOT = SCAFFOLD_ROOT
PACKAGE_DIR = Path(__file__).resolve().parent
DOCS_DIR = SCAFFOLD_ROOT / "docs"
IMPORTS_DIR = SCAFFOLD_ROOT / "imports"
CURRENT_BASELINE_DIR = IMPORTS_DIR / "current_baseline"
LOCKS_DIR = SCAFFOLD_ROOT / "locks"
LOGS_DIR = SCAFFOLD_ROOT / "logs"
MANIFESTS_DIR = SCAFFOLD_ROOT / "manifests"
PATCHES_DIR = MANIFESTS_DIR / "patches"
OUTPUTS_DIR = SCAFFOLD_ROOT / "outputs"
SANDBOXES_DIR = SCAFFOLD_ROOT / "sandboxes"
WORKSPACES_DIR = SCAFFOLD_ROOT / "workspaces"
PROMPTS_DIR = SCAFFOLD_ROOT / "prompts"
BUILDS_DIR = OUTPUTS_DIR / "builds"
BUILD_SOURCES_DIR = OUTPUTS_DIR / "build_sources"
PATCH_OUTPUTS_DIR = OUTPUTS_DIR / "patches"
REVIEW_FEEDBACK_DIR = OUTPUTS_DIR / "review_feedback"
REPORT_REVISION_CANDIDATES_DIR = OUTPUTS_DIR / "report_revision_candidates"
VALIDATION_DIR = OUTPUTS_DIR / "validation"
FONT_AUDIT_DIR = OUTPUTS_DIR / "font_audit"
OUTLINE_AUDIT_DIR = OUTPUTS_DIR / "outline_audit"
CANDIDATE_AUDIT_DIR = OUTPUTS_DIR / "candidate_audit"
SNIPPET_AUDIT_DIR = OUTPUTS_DIR / "snippet_audit"
SECTION_PIPELINE_DIR = OUTPUTS_DIR / "section_pipeline"
PUBLISHED_DIR = OUTPUTS_DIR / "published"
SOURCES_DIR = SCAFFOLD_ROOT / "sources"
TESTS_DIR = SCAFFOLD_ROOT / "tests"
TRACE_DIR = PROJECT_ROOT / "trace"
DESKTOP_DIR = PROJECT_ROOT / "desktop"
DEFAULT_OFFICEX_DESKTOP_SHELL_DIR = DESKTOP_DIR

DEFAULT_BASELINE_MANIFEST = MANIFESTS_DIR / "baseline.yml"
DEFAULT_SECTIONS_MANIFEST = MANIFESTS_DIR / "sections.yml"
DEFAULT_FIGURES_MANIFEST = MANIFESTS_DIR / "figures.yml"
DEFAULT_SNIPPETS_MANIFEST = MANIFESTS_DIR / "snippets.yml"
DEFAULT_CITATIONS_MANIFEST = MANIFESTS_DIR / "citations.yml"
DEFAULT_NAVIGATION_CATALOG = MANIFESTS_DIR / "navigation_catalog.yml"
DEFAULT_AGENT_CATALOG_MANIFEST = MANIFESTS_DIR / "agent_catalog.yml"
DEFAULT_PROVIDER_CATALOG_MANIFEST = MANIFESTS_DIR / "provider_catalog.yml"
DEFAULT_TEMPLATE_PROFILE_MANIFEST = MANIFESTS_DIR / "template_profile.yml"
DEFAULT_LAYOUT_CONTRACT_MANIFEST = MANIFESTS_DIR / "layout_contract.yml"
DEFAULT_WRITE_CONTRACT_MANIFEST = MANIFESTS_DIR / "write_contract.yml"
DEFAULT_BUILD_SOURCE = SOURCES_DIR / "minimal_build.yml"
DEFAULT_SECTION_BUILD_SOURCE = BUILD_SOURCES_DIR / "assembled_sections_build.yml"
