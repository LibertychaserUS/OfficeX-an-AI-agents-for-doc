# Revision Manifests

This directory stores revision-sidecar configuration and generated patch specs
that belong to candidate-only controlled patch workflows.

These artifacts are separate from:

- build-source manifests
- write contracts
- canonical publication metadata

Revision patch specs should be treated as executable candidate patch plans, not
as build-mode sources of truth.

